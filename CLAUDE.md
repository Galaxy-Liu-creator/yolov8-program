# Claude Code 入口

Claude Code 新开窗口会自动读取本文件。本仓库的长期项目事实、数据路径、训练状态、业务口径和维护规则统一维护在：

- `AGENTS.md`

请立即读取并遵守 `AGENTS.md`。进入 `backend-train-model/`、`docs/`、`inspection-flask/` 等子目录工作时，还要读取该目录下更近的 `AGENTS.md`。

本文件只作为 Claude 的自动入口，不重复维护项目内容；如项目长期信息发生变化，请更新对应 `AGENTS.md`，不要把事实复制到这里。
