# D15_20260119061405 人工复核记录（5.10 阶段）

## 1. 本轮定位

这条序列在 `5.10` 不作为 crowded / overlap 主推进入口，而是作为“可见性弱型”对照序列保留。

## 2. 今天建议只抽看

- `D15_20260119061405_frame_0346`

## 3. 今天要回答的问题

1. 它是否仍然稳定属于可见性弱型；
2. 能否帮助你防止把 crowded / overlap 与可见性弱重新混写；
3. 是否存在需要单独标注修订的明确证据。

## 4. 记录提醒

- 本轮不建议在这条序列上扩看太多图；
- 它的主要作用是给 crowded / overlap 主线提供对照；
- 如果没有新证据，不需要把 5.7 已有语义结论重写一遍。

## 5. 字段总览表（中文解释 + 应该怎么填 + 示例）

> 下面的“示例值”只是**示范写法**，不代表本对照序列当前已经得出的真实结论。


| 字段                   | 中文意思          | 这一栏要填什么                                             | 常见取值 / 写法                                             | 示例（仅示范格式）                         |
| -------------------- | ------------- | --------------------------------------------------- | ----------------------------------------------------- | --------------------------------- |
| `semantic_primary`   | 主语义标签         | 写这张对照帧在语义层首先更稳定属于哪条主线。                              | `visibility_weak` / `crowded_or_overlap` / 其他自定义中文说明  | `visibility_weak`                 |
| `semantic_secondary` | 次语义标签         | 写附加现象；没有就留空。                                        | `dark_background` / `occluded` / `pose_abnormal` / 留空 | `dark_background`                 |
| `need_relabel`       | 是否怀疑需要修框 / 重标 | 只判断“要不要进一步修标”，不要和 `relabel_status` 混写。              | `true` / `false` / `null`                             | `false`                           |
| `relabel_status`     | 重标状态          | 写当前标注处理状态。                                          | `not_needed` / `suspected` / `pending` / `fixed`      | `not_needed`                      |
| `对 crowded 主线的区分作用`  | 对照意义          | 写这张图为什么更像“可见性弱型”，以及它怎样帮助你避免把它误判成 crowded / overlap。 | 1~3 句中文                                               | `本帧核心是暗背景+姿态导致的人体线索弱，不是近邻目标黏连。`   |
| `主要依据`               | 判断证据          | 必须拆成原图、`baseline_val`、`img768_val`、GT/标签四块来写。       | 短句，1~3 句每块                                            | `img768 有改善但仍召回不足，说明主要矛盾不在拥挤分离。`  |
| `当前一句话结论`            | 压缩结论          | 用一句中文概括这张对照帧的最终作用。                                  | 1 句中文                                                 | `该帧仍稳定属于可见性弱型，对 crowded 主线起区分作用。` |


## 6. 推荐填写顺序（避免把对照帧写成 crowded 主线）


| 步骤  | 先看什么                   | 要解决的问题                             | 输出到哪里                          |
| --- | ---------------------- | ---------------------------------- | ------------------------------ |
| 1   | 原图                     | 暗背景、姿态异常、遮挡、人体可见区域到底弱在哪里           | `主要依据 -> 原图观察`                 |
| 2   | `baseline_val` overlay | 是完全无响应、局部弱响应，还是有明显 crowded 合框现象    | `主要依据 -> baseline_val overlay` |
| 3   | `img768_val` overlay   | 放大后有没有改善，如果改善有限意味着什么               | `主要依据 -> img768_val overlay`   |
| 4   | `labels/` GT           | GT 是否合理，有没有明显需要修标的证据               | `主要依据 -> GT / 标签检查`            |
| 5   | 汇总结论                   | 它是否仍然稳属可见性弱型，以及对 crowded 主线有什么区分作用 | 各字段 + 一句话结论                    |


## 7. 对照帧记录模版（表格版，先填这里，再决定是否需要同步到 summary）

### D15_20260119061405_frame_0346

#### 7.1 字段填写表


| 项目                   | 这一格要填什么                                          | 你的填写                           |
| -------------------- | ------------------------------------------------ | ------------------------------ |
| `semantic_primary`   | 主语义标签，说明这张图更稳定属于哪条语义主线                           | visibility_weak                |
| `semantic_secondary` | 次语义标签，补充暗光 / 遮挡 / 姿态异常等现象；可留空                    |                                |
| `need_relabel`       | `true` / `false` / `null`                        | false                          |
| `relabel_status`     | `not_needed` / `suspected` / `pending` / `fixed` |                                |
| `对 crowded 主线的区分作用`  | 写清它为什么更像可见性弱型，而不是 crowded / overlap              | 有完整人形但没识别出来                    |
| `当前一句话结论`            | 一句中文压缩结论                                         | 该帧仍稳定属于可见性弱型，对 crowded 主线起区分作用 |


#### 7.2 主要依据填写表


| 证据来源                   | 这一格建议怎么写                   | 你的填写  |
| ---------------------- | -------------------------- | ----- |
| 原图观察                   | 写暗背景、姿态、遮挡、人体可见面积和局部线索弱在哪里 | 姿态笔直  |
| `baseline_val` overlay | 写是完全无响应、弱响应，还是别的情况         | 完全无响应 |
| `img768_val` overlay   | 写放大后是否改善、改善幅度是否有限          | 否     |
| GT / 标签检查              | 写 GT 是否合理、是否存在明显标注问题       | 合理    |


#### 7.3 示例（仅示范写法，不代表本帧真实结论）


| 项目                     | 示例填写                                     |
| ---------------------- | ---------------------------------------- |
| `semantic_primary`     | `visibility_weak`                        |
| `semantic_secondary`   | `dark_background`                        |
| `need_relabel`         | `false`                                  |
| `relabel_status`       | `not_needed`                             |
| `对 crowded 主线的区分作用`    | 这张图的主问题更像暗背景和姿态导致的人体线索不足，而不是两个人贴得太近。     |
| 原图观察                   | 目标人体与背景对比度低，肢体姿态也不完整，可见轮廓较弱。             |
| `baseline_val` overlay | 基本是弱响应或无响应，不像 crowded 场景那样出现明显合并大框。      |
| `img768_val` overlay   | 放大后有一定改善，但提升有限，说明主矛盾不在分离近邻目标。            |
| GT / 标签检查              | GT 看起来基本合理，暂无必须修标的证据。                    |
| `当前一句话结论`              | 该帧仍稳定属于可见性弱型，对 crowded 主线主要起“不要混写”的对照作用。 |


## 8. 对照序列结论（抽看完后再写）

### 8.1 对照序列结论表


| 项目                              | 这一格要填什么                             | 你的填写                                            |
| ------------------------------- | ----------------------------------- | ----------------------------------------------- |
| 是否仍然稳定属于“可见性弱型”对照主线             | 写“是 / 否 / 暂不确定”                     | 是                                               |
| 与 crowded / overlap 主线最关键的差别是什么 | 用 1~2 句中文写清楚                        | 更核心的问题是人体自身可见性弱、背景干扰和姿态因素，而不是多人近邻重叠后的分离失败。      |
| 是否存在需要单独进入修框 / 重标流程的明确证据        | 写“有 / 没有 / 不确定”，并说明原因               | 没有；当前未见必须优先进入修标流程的直接证据。                         |
| 当前更建议它在后续阶段扮演什么角色               | `继续只做对照` / `需要扩看` / `需要单独修标`，并补一句原因 | `继续只做对照`；当前主要用于约束 crowded / overlap 主线的归因边界。    |
| 当前一句对照序列总结                      | 面向 `summary` 的压缩结论                  | 本序列仍稳定承担“可见性弱型”对照角色，不应与 crowded / overlap 主线混写。 |


### 8.2 对照序列示例（仅示范格式）


| 项目                              | 示例填写                                            |
| ------------------------------- | ----------------------------------------------- |
| 是否仍然稳定属于“可见性弱型”对照主线             | 是                                               |
| 与 crowded / overlap 主线最关键的差别是什么 | 核心是目标自身视觉线索弱，而不是多人近邻重叠后难分开。                     |
| 是否存在需要单独进入修框 / 重标流程的明确证据        | 没有，目前不足以单独走修标。                                  |
| 当前更建议它在后续阶段扮演什么角色               | `继续只做对照`；主要用于约束 crowded 主线的归因边界。                |
| 当前一句对照序列总结                      | 本序列仍稳定承担“可见性弱型”对照角色，不应与 crowded / overlap 主线混写。 |


