# D05_20260123074841 人工复核记录（5.13 阶段）

## 1. 本轮定位

这一轮把 `5.10` 已有填写内容正式沉淀为 `5.13` 阶段结论，不再把新阶段的总结继续写回 `5.10` 当天的 notes。

## 2. 今天优先看的帧

- `D05_20260123074841_frame_0026`
- `D05_20260123074841_frame_0029`
- `D05_20260123074841_frame_0030`

## 3. 今天要回答的问题

1. 当前是否已经稳定属于 crowded / overlap 主线；是，可与 `D15_20260119203927` 一起作为 crowded 主线核心序列。
2. 当前更稳的主机制是什么；当前更偏 `second_person_no_response`，遮挡叠加是稳定伴随因素，但当前不足以把 `weak_box_match_fail` 升级为主机制。
3. 当前有哪些问题只是 notes 口径不够整齐，但不阻塞推进；顶部问答偏“一框合两人”、`0029` 字段错位，但这些都还不需要先返工到会打断 `5.13` 正式收口。
4. 下一步更建议优先补什么证据；优先补 `0029 / 0030` 的 overlay 级短句，继续确认哪些帧只是“第二人无响应”，哪些帧仍保留“一框合两人”嫌疑。

## 4. 记录提醒

- 本轮目标是把 `5.10` 已形成的判断正式收口到 `5.13`，不是再重写一轮逐帧原始观察。
- 如果 `5.10` 顶部问答、逐帧填写和当前正式收口口径不完全一致，以 `5.13` 的独立收口结论为准，不回写历史阶段。
- 先把主机制、修标判断、下一步动作写清楚，再处理 notes 的局部工整性问题。
- 在没有更强新证据前，不要直接把这条 sequence 翻译成 NMS、`ioa20` 或多变量训练动作。

## 5. 字段总览表（5.13 正式收口版）

| 字段 | 中文意思 | 这一栏在 5.13 应该怎么填 | 当前正式写法 |
| --- | --- | --- | --- |
| `semantic_primary` | 主语义标签 | 继续承接 `5.10 / 5.7` 已稳定的主语义标签 | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签 | 有稳定附加语义就写，没有可留空 | `0030` 可写 `occluded`，其余可留空 |
| `mechanism_primary` | 主失败机制 | 只写当前最稳的主机制 | `second_person_no_response` |
| `mechanism_secondary` | 次失败机制 / 保留分支 | 单帧无稳定次机制可留空；sequence 级可记录保留分支 | 单帧留空；sequence 级保留 `merge_two_people` 待核验 |
| `mechanism_confidence` | 主机制判断把握度 | 按现有 notes 一致性填写 `high / medium / low` | `high` |
| `need_relabel` | 是否需要优先修标 | 只判断要不要进入修标流程 | `false` |
| `relabel_status` | 修标状态 | 写当前修标状态 | `not_needed` |
| `non_blocking_issues` | 已发现但不阻塞的问题 | 写 notes 里不够整齐但不影响推进的点 | 顶部问答偏“一框合两人”、`0029` 字段错位 |
| `current_summary` | 当前一句压缩结论 | 方便直接抄到 `summary` / `manifest` | 本 sequence 当前稳定支撑 crowded / overlap 主线，主机制更偏第二人无响应，遮挡叠加为稳定伴随因素。 |

## 6. 推荐填写顺序（承接 5.10 -> 5.13）

| 步骤 | 先看什么 | 要解决的问题 | 输出到哪里 |
| --- | --- | --- | --- |
| 1 | `5-10notes.md` 顶部问答与 sequence 级结论 | 当前主线和主机制是不是已经足够稳定 | 本页第 3、8 节 |
| 2 | `5-10notes.md` 的逐帧记录 | 哪些问题只是表述不整齐，哪些问题真的会推翻主机制 | 本页第 7、8 节 |
| 3 | 当前 `5.13` 阶段 summary / manifest 口径 | 是否需要把结论压成统一短句 | 本页第 8.2 节 |
| 4 | 下一轮任务判断 | 下一步先补证据还是先改训练参数 | 本页第 9、10 节 |

## 7. 逐帧正式收口记录表（表格版，先填这里，再同步 manifest）

### D05_20260123074841_frame_0026

#### 7.1 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签；继续承接 `5.10` | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签；没有可留空 |  |
| `mechanism_primary` | 当前最稳的主失败机制 | `second_person_no_response` |
| `mechanism_secondary` | 次机制；没有可留空 |  |
| `mechanism_confidence` | `low / medium / high` | `high` |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `当前一句话结论` | 一句中文压缩结论 | 两人贴近且前后遮挡，第二人几乎不可见，当前更偏第二人无独立有效响应。 |

#### 7.2 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写两人贴近程度、遮挡关系、第二人可见度 | 两人贴得很近，前后遮挡明显，第二人几乎不可见。 |
| `baseline_val` overlay | 写第二人附近有没有独立有效框 | 未见第二人独立有效响应。 |
| `img768_val` overlay | 写放大后是否出现实质改善 | 放大后仍未形成稳定独立响应。 |
| GT / 标签检查 | 写 GT 是否合理、是否有修标直接证据 | 当前未见必须优先修标的直接证据。 |

### D05_20260123074841_frame_0029

#### 7.3 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签；继续承接 `5.10` | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签；没有可留空 |  |
| `mechanism_primary` | 当前最稳的主失败机制 | `second_person_no_response` |
| `mechanism_secondary` | 次机制；没有可留空 |  |
| `mechanism_confidence` | `low / medium / high` | `high` |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `当前一句话结论` | 一句中文压缩结论 | 当前仍更偏第二人无响应；字段错位属于 notes 整理问题，不改变 crowded 主线正式收口方向。 |

#### 7.4 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写遮挡形态和第二人可见度 | 前后遮挡仍然明显，第二人可见线索较弱。 |
| `baseline_val` overlay | 写第二人附近是否完全无框或只有弱响应 | 当前仍未见稳定独立有效响应。 |
| `img768_val` overlay | 写放大后是否出现实质改善 | 放大后仍不足以形成稳定独立响应。 |
| GT / 标签检查 | 写字段错位是否等于标注问题 | `0029` 当前主要是 notes 字段错位，不构成修标直接证据。 |

### D05_20260123074841_frame_0030

#### 7.5 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签；继续承接 `5.10` | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签；有稳定附加语义可写 | `occluded` |
| `mechanism_primary` | 当前最稳的主失败机制 | `second_person_no_response` |
| `mechanism_secondary` | 次机制；没有可留空 |  |
| `mechanism_confidence` | `low / medium / high` | `high` |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `当前一句话结论` | 一句中文压缩结论 | 遮挡叠加继续存在，第二人仍未形成独立有效响应，当前未见修标优先级证据。 |

#### 7.6 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写遮挡是否持续存在、第二人轮廓是否仍弱 | 遮挡叠加继续存在，第二人轮廓仍较弱。 |
| `baseline_val` overlay | 写第二人是否彻底无响应 | 第二人仍未形成独立有效响应。 |
| `img768_val` overlay | 写放大后是否从无响应变成稳定弱响应 | 放大后仍未出现足以改变主结论的稳定独立响应。 |
| GT / 标签检查 | 写 GT 是否有修标直接证据 | 当前未见修标优先级证据。 |

## 8. sequence 级正式结论（三帧收口后再同步 summary）

### 8.1 sequence 级结论表

| 项目 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 是否稳定属于 crowded / overlap 主线 | 写“是 / 否 / 暂不确定” | 是 |
| 本 sequence 主机制更偏 | 写主机制名，并补一句中文解释 | `second_person_no_response`；当前整体更像第二个人缺少独立有效响应。 |
| 当前保留的伴随分支 | 写仍需继续核验但未升级为主机制的分支 | `merge_two_people`；仍可保留嫌疑，但不足以压过主机制。 |
| 是否存在必须优先修标的直接证据 | 写“有 / 没有 / 暂不确定”并说明原因 | 没有；当前未见必须优先修框或重标的直接证据。 |
| 是否有会阻塞下一阶段推进的问题 | 写“有 / 没有”并说明原因 | 没有；顶部问答偏“一框合两人”、`0029` 字段错位都属于不阻塞推进的问题。 |
| 与 `D15_20260119203927` 是否可稳定归入同一条机制主线 | 写“能 / 不能 / 暂不确定” | 能；两条 sequence 当前都稳定支撑 crowded 主线。 |
| 当前更建议的下一步 | 写最优先动作 | 补 `0029 / 0030` overlay 级短句，继续区分“第二人无响应”和“合框嫌疑”。 |
| 当前一句 sequence 级总结 | 面向 `summary` 的压缩结论 | 本 sequence 当前稳定支撑 crowded / overlap 主线，主机制更偏第二人无响应，遮挡叠加为稳定伴随因素。 |

### 8.2 可直接同步到 summary / manifest 的固定短句

- `D05_20260123074841_frame_0026`：两人贴近且前后遮挡，第二人几乎不可见，当前更偏第二人无独立有效响应。
- `D05_20260123074841_frame_0029`：当前仍更偏第二人无响应；字段错位属于 notes 整理问题，不改变 crowded 主线正式收口方向。
- `D05_20260123074841_frame_0030`：遮挡叠加继续存在，第二人仍未形成独立有效响应，当前未见修标优先级证据。

## 9. 下一步更建议先做什么

1. 优先补 `0029 / 0030` 的 overlay 级短句；
2. 继续确认哪些帧只是“第二人无响应”，哪些帧仍保留“一框合两人”嫌疑；
3. 在没有更强新证据前，不建议先跳 NMS、`ioa20` 或多变量训练。

## 10. 当前明确不建议优先做什么

- 不建议因为顶部问答里出现“一框合两人”，就把整条 sequence 直接改写成统一 `merge_two_people` 结论；
- 不建议为了修 `0029` 的表格错位而打断 crowded 主线正式收口；
- 不建议在当前证据仍主要支持 `second_person_no_response` 时，先跳到 NMS、`ioa20` 或多变量训练。
