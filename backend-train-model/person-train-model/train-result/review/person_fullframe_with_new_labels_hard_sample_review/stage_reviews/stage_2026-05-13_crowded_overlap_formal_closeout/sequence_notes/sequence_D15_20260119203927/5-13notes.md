# D15_20260119203927 人工复核记录（5.13 阶段）

## 1. 本轮定位

这一轮不再继续往 `5.10` 当天的 notes 里塞新的阶段结论，而是把 `5.10` 已有填写内容正式收口为 `5.13` 阶段结论。

## 2. 今天优先看的帧

- `D15_20260119203927_frame_0143`
- `D15_20260119203927_frame_0180`

## 3. 今天要回答的问题

1. 当前是否已经稳定属于 crowded / overlap 主线；是，与 `D05_20260123074841` 一起构成 crowded 主线的两条核心序列。
2. 当前更稳的主机制是什么；现有 notes 更支持 `second_person_no_response`，而不是先统一改写成 `merge_two_people`。
3. 当前有哪些问题只是 notes 口径不够整齐，但不阻塞推进；`0143 / 0180` 目前主要依靠 sequence 级结论先收口，单帧字段表还可以继续补细，但不影响 `5.13` 正式结论先独立成立。
4. 下一步更建议优先补什么证据；优先补 `0143 / 0180` 的 overlay 级短句，继续确认是否始终都是“第二人无响应”，还是个别帧仍保留“一框合两人”嫌疑。

## 4. 记录提醒

- 本轮目标是把 `5.10` 已有结论正式收口到 `5.13`，不是再重写一轮 crowded / overlap 的语义归类。
- 当前这条 sequence 允许先按 sequence 级结论保守收口，不要求今天就把每个单帧表都补到和 `D05` 一样细。
- 若旧记录里残留“物体遮挡”或“合框嫌疑”描述，应先作为边界保留，不直接改写主机制。
- 在没有更强新证据前，不要直接把这条 sequence 翻译成 NMS、`ioa20` 或修标入口。

## 5. 字段总览表（5.13 正式收口版）

| 字段 | 中文意思 | 这一栏在 5.13 应该怎么填 | 当前正式写法 |
| --- | --- | --- | --- |
| `semantic_primary` | 主语义标签 | 继续承接 `5.10 / 5.7` 已稳定的主语义标签 | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签 | 有稳定附加语义就写，没有可留空 | 当前两帧都可留空 |
| `mechanism_primary` | 主失败机制 | 只写当前最稳的主机制 | `second_person_no_response` |
| `mechanism_secondary` | 次失败机制 / 保留分支 | 单帧无稳定次机制可留空；sequence 级可记录保留分支 | 单帧留空；sequence 级保留 `merge_two_people` 待核验 |
| `mechanism_confidence` | 主机制判断把握度 | 按现有 notes 一致性填写 `high / medium / low` | `medium` |
| `need_relabel` | 是否需要优先修标 | 只判断要不要进入修标流程 | `false` |
| `relabel_status` | 修标状态 | 写当前修标状态 | `not_needed` |
| `non_blocking_issues` | 已发现但不阻塞的问题 | 写 notes 里不够整齐但不影响推进的点 | `0143 / 0180` 当前主要按 sequence 级结论收口 |
| `current_summary` | 当前一句压缩结论 | 方便直接抄到 `summary` / `manifest` | 本 sequence 当前稳定属于 crowded / overlap 主线，主机制更偏第二人无响应，但仍保留少量“一框合两人”待核验边界。 |

## 6. 推荐填写顺序（承接 5.10 -> 5.13）

| 步骤 | 先看什么 | 要解决的问题 | 输出到哪里 |
| --- | --- | --- | --- |
| 1 | `5-10notes.md` 的 sequence 级结论 | 当前主线和主机制是不是已经足够稳定 | 本页第 3、8 节 |
| 2 | `5-10notes.md` 中 `0143 / 0180` 的已有描述 | 现有证据是支持“第二人无响应”，还是必须改写成别的机制 | 本页第 7、8 节 |
| 3 | 当前 `5.13` 阶段 summary / manifest 口径 | 是否需要压成统一短句 | 本页第 8.2 节 |
| 4 | 下一轮任务判断 | 下一步先补证据还是先改训练参数 | 本页第 9、10 节 |

## 7. 逐帧正式收口记录表（表格版，先填这里，再同步 manifest）

### D15_20260119203927_frame_0143

#### 7.1 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签；继续承接 `5.10` | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签；没有可留空 |  |
| `mechanism_primary` | 当前最稳的主失败机制 | `second_person_no_response` |
| `mechanism_secondary` | 次机制；没有可留空 |  |
| `mechanism_confidence` | `low / medium / high` | `medium` |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `当前一句话结论` | 一句中文压缩结论 | 当前更偏第二人缺少独立有效响应，尚未看到足以升级为“弱框匹配失败”或“修标优先”的稳定证据。 |

#### 7.2 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写两人相对位置、第二人可见度和拥挤程度 | 当前仍可归入 crowded 主线，第二人可见区域较弱。 |
| `baseline_val` overlay | 写第二人附近有没有独立有效框 | 当前未见稳定独立有效响应。 |
| `img768_val` overlay | 写放大后是否出现实质改善 | 放大后仍不足以把主结论改写成别的机制。 |
| GT / 标签检查 | 写 GT 是否合理、是否有修标直接证据 | 当前未见必须优先修标的直接证据。 |

### D15_20260119203927_frame_0180

#### 7.3 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签；继续承接 `5.10` | `crowded_or_overlap` |
| `semantic_secondary` | 次语义标签；没有可留空 |  |
| `mechanism_primary` | 当前最稳的主失败机制 | `second_person_no_response` |
| `mechanism_secondary` | 次机制；没有可留空 |  |
| `mechanism_confidence` | `low / medium / high` | `medium` |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `当前一句话结论` | 一句中文压缩结论 | 本帧当前继续按 crowded 主线的 sequence 级结论保守收口，下一步优先补 overlay 级短句，不先改写主机制。 |

#### 7.4 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写本帧的遮挡形态和第二人可见线索 | 本帧存在场景遮挡因素，但当前仍不足以推翻 crowded 主线收口方向。 |
| `baseline_val` overlay | 写是否存在稳定独立框或弱框 | 当前未见足以升级为“弱框匹配失败”的稳定响应。 |
| `img768_val` overlay | 写放大后是否出现实质改善 | 放大后仍未形成足以改变主结论的稳定独立响应。 |
| GT / 标签检查 | 写 GT 是否合理、是否有修标直接证据 | 当前未见必须优先修标的直接证据。 |

## 8. sequence 级正式结论（两帧收口后再同步 summary）

### 8.1 sequence 级结论表

| 项目 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 是否稳定属于 crowded / overlap 主线 | 写“是 / 否 / 暂不确定” | 是 |
| 本 sequence 主机制更偏 | 写主机制名，并补一句中文解释 | `second_person_no_response`；当前更像第二个人缺少独立有效响应。 |
| 当前保留的伴随分支 | 写仍需继续核验但未升级为主机制的分支 | `merge_two_people`；个别帧仍可保留此嫌疑，但当前不足以升级为统一主机制。 |
| 是否存在必须优先修标的直接证据 | 写“有 / 没有 / 暂不确定”并说明原因 | 没有；当前未见必须优先修框或重标的直接证据。 |
| 是否有会阻塞下一阶段推进的问题 | 写“有 / 没有”并说明原因 | 没有；单帧字段还不够细，但不影响当前主线先收口。 |
| 与 `D05_20260123074841` 是否可稳定归入同一条机制主线 | 写“能 / 不能 / 暂不确定” | 能；两条 sequence 当前都稳定支撑 crowded 主线。 |
| 当前更建议的下一步 | 写最优先动作 | 补 `0143 / 0180` overlay 级短句，继续确认是否始终都是“第二人无响应”。 |
| 当前一句 sequence 级总结 | 面向 `summary` 的压缩结论 | 本 sequence 当前稳定属于 crowded / overlap 主线，主机制更偏第二人无响应，但仍保留少量“一框合两人”待核验边界。 |

### 8.2 可直接同步到 summary / manifest 的固定短句

- `D15_20260119203927_frame_0143`：当前更偏第二人缺少独立有效响应，尚未看到足以升级为“弱框匹配失败”或“修标优先”的稳定证据。
- `D15_20260119203927_frame_0180`：本帧当前继续按 crowded 主线的 sequence 级结论保守收口，下一步优先补 overlay 级短句，不先改写主机制。

## 9. 下一步更建议先做什么

1. 优先补 `0143 / 0180` 的 overlay 级短句；
2. 继续确认是否始终都是“第二人无响应”，还是个别帧仍保留“一框合两人”嫌疑；
3. 在没有新证据前，不建议先跳 NMS、`ioa20` 或修标分支。

## 10. 当前明确不建议优先做什么

- 不建议因为个别旧表述里出现遮挡或合框嫌疑，就把整条 sequence 直接改写成别的主机制；
- 不建议为了把 `0143 / 0180` 的单帧表格补得更细，就继续拖延 crowded 主线正式收口；
- 不建议在当前证据仍主要支持 `second_person_no_response` 时，先跳到 NMS、`ioa20` 或修标分支。
