# D15_20260119061405 人工复核记录（5.13 阶段）

## 1. 本轮定位

这条序列在 `5.13` 仍继续只作为“可见性弱型”对照序列保留，用来约束 crowded / overlap 主线的归因边界。

## 2. 今天建议只抽看

- `D15_20260119061405_frame_0346`

## 3. 今天要回答的问题

1. 是否继续只作为“可见性弱型”对照序列保留；是，不应重新并回 crowded / overlap 主线。
2. 当前是否已经出现需要优先修标或新开实验的稳定证据；否，现有记录不足以支持把它升级成当前最优先处理入口。
3. 当前有哪些问题只是 notes 口径不够整齐，但不阻塞推进；局部措辞和个别栏位写法还不够整齐，但这些问题暂时不影响它继续承担对照角色。
4. 下一步更建议优先补什么证据；暂不扩看，只保留 `0346` 对照短句，只有当 crowded 主线需要继续校正边界时，再回头补更细说明。

## 4. 记录提醒

- 本轮目标是保留这条 sequence 的对照作用，不是把它重新并回 crowded / overlap 主线。
- 如果现有 notes 里有局部表述不够整齐，先记录为“不阻塞推进”的问题，不要打断 crowded 主线正式收口。
- 当前不建议在这条对照序列上扩看太多图，也不建议直接把个别疑点升级成修标或新实验入口。
- 只有当 crowded 主线后续需要重新校正归因边界时，才回头补更细的对照说明。

## 5. 字段总览表（5.13 对照收口版）

| 字段 | 中文意思 | 这一栏在 5.13 应该怎么填 | 当前正式写法 |
| --- | --- | --- | --- |
| `semantic_primary` | 主语义标签 | 写这条对照帧当前更稳定属于哪条对照主线 | `visibility_weak` |
| `semantic_secondary` | 次语义标签 | 有稳定附加现象就写，没有可留空 | 当前可留空 |
| `need_relabel` | 是否需要优先修标 | 只判断要不要进入修标流程 | `false` |
| `relabel_status` | 修标状态 | 写当前修标状态 | `not_needed` |
| `control_purpose` | 对 crowded 主线的区分作用 | 写它为什么仍只做对照 | 用来约束 crowded / overlap 主线归因边界 |
| `non_blocking_issues` | 已发现但不阻塞的问题 | 写 notes 里不够整齐但不影响推进的点 | 局部措辞和个别栏位写法还不够整齐 |
| `current_summary` | 当前一句压缩结论 | 方便直接抄到 `summary` | 本 sequence 当前仍稳定承担“可见性弱型”对照角色，不应与 crowded / overlap 主线混写。 |

## 6. 推荐填写顺序（承接 5.10 -> 5.13）

| 步骤 | 先看什么 | 要解决的问题 | 输出到哪里 |
| --- | --- | --- | --- |
| 1 | `5-10notes.md` 的对照序列结论 | 它是否仍稳定属于 `visibility_weak` | 本页第 3、8 节 |
| 2 | `5-10notes.md` 中 `0346` 的已有描述 | 有没有足以升级成修标或新实验的硬证据 | 本页第 7、8 节 |
| 3 | 当前 `5.13` 阶段 summary 口径 | 是否需要把对照作用压成统一短句 | 本页第 8.2 节 |
| 4 | 下一轮任务判断 | 这条对照序列是否会抢 crowded 主线节奏 | 本页第 9、10 节 |

## 7. 对照帧正式收口记录表（表格版，先填这里，再决定是否同步 summary）

### D15_20260119061405_frame_0346

#### 7.1 字段填写表

| 项目 | 这一格在 5.13 应该怎么填 | 当前正式填写 |
| --- | --- | --- |
| `semantic_primary` | 主语义标签，说明这张图更稳定属于哪条对照主线 | `visibility_weak` |
| `semantic_secondary` | 次语义标签；没有可留空 |  |
| `need_relabel` | `true / false / null` | `false` |
| `relabel_status` | `not_needed / suspected / pending / fixed` | `not_needed` |
| `对 crowded 主线的区分作用` | 写清它为什么更像可见性弱型，而不是 crowded / overlap | 当前继续只作为“可见性弱型”对照帧保留。 |
| `当前一句话结论` | 一句中文压缩结论 | 本帧当前继续只作为“可见性弱型”对照帧保留，不应重新并回 crowded / overlap 主线。 |

#### 7.2 主要依据填写表

| 证据来源 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 原图观察 | 写目标可见性为什么更弱、为什么不像 crowded | 当前更像目标可见性弱，而不是多人近邻重叠后的分离失败。 |
| `baseline_val` overlay | 写是完全无响应、弱响应，还是明显 crowded 合框 | 当前更接近无响应 / 弱响应，不像 crowded 合框。 |
| `img768_val` overlay | 写放大后是否出现决定性改善 | 放大后也没有出现足以把它升级成新实验入口的决定性改善。 |
| GT / 标签检查 | 写是否存在必须优先修标的直接证据 | 现有记录不足以支持把它升级成修标主入口。 |

## 8. 对照序列正式结论（抽看完后再同步 summary）

### 8.1 对照序列结论表

| 项目 | 这一格在 5.13 应该怎么写 | 当前正式填写 |
| --- | --- | --- |
| 是否仍稳定属于“可见性弱型”对照主线 | 写“是 / 否 / 暂不确定” | 是 |
| 与 crowded / overlap 主线最关键的差别是什么 | 用 1~2 句中文写清楚 | 更核心的问题是目标可见性弱，而不是多人近邻重叠后的分离失败。 |
| 是否存在必须优先进入修标流程的直接证据 | 写“有 / 没有 / 暂不确定”并说明原因 | 没有；现有记录不足以支持把它升级成修标主入口。 |
| 当前是否会阻塞下一阶段推进 | 写“有 / 没有”并说明原因 | 没有；它当前不是 crowded 主线训练入口的阻塞项。 |
| 当前更建议它在后续阶段扮演什么角色 | 写最优先动作 | 继续只做对照；只有当 crowded 主线需要校正边界时再回头补细。 |
| 当前一句对照序列总结 | 面向 `summary` 的压缩结论 | 本 sequence 当前仍稳定承担“可见性弱型”对照角色，不应与 crowded / overlap 主线混写。 |

### 8.2 可直接同步到 summary 的固定短句

- `D15_20260119061405_frame_0346`：当前继续只作为“可见性弱型”对照帧保留，作用是帮助区分 crowded 主线与 visibility weak，而不是重开 crowded 收口。

## 9. 下一步更建议先做什么

1. 继续把它保留为对照序列；
2. 只有当 crowded 主线后续需要进一步校正归因边界时，再回头补更细的对照说明；
3. 当前不建议先把资源放到这条序列的 notes 精修上。

## 10. 当前明确不建议优先做什么

- 不建议为了修这条对照记录的局部措辞，就打断 crowded 主线正式收口；
- 不建议在 crowded 主线还没完成下一轮动作分流前，就先把训练资源转到这条对照序列上；
- 不建议仅凭个别旧记录里的疑点，就把它直接升级成修标或新实验主入口。
